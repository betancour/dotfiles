# ~/.gitconfig.local — personal Git identity (not tracked in dotfiles)
# Copy to ~/.gitconfig.local and customize:
#   cp ~/Development/dotfiles/.gitconfig.local.template ~/.gitconfig.local

[user]
    name = Your Name
    email = you@example.com
    username = your-github-username