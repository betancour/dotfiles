-- Options are automatically loaded before lazy.nvim startup
-- Default options that are always set: https://github.com/LazyVim/LazyVim/blob/main/lua/lazyvim/config/options.lua
-- Add any additional options here

-- EditorConfig: built into Neovim 0.9+ (runtime plugin/editorconfig.lua).
-- Applies .editorconfig from the file's directory up to root, overriding
-- buffer-local indent/format options (indent_style, indent_size, etc.).
-- Explicitly enable so it stays on even if a future default changes.
vim.g.editorconfig = true
