# .bash_login — post-interactive login setup
. "${BASH_SOURCE[0]%/*}/../lib/bootstrap.sh"

case $- in *l*) ;; *) return ;; esac

dotfiles_source_once "${DOTFILES_LIB_DIR}/login.sh"
dotfiles_login

[ -r "$HOME/.bash_login.local" ] && . "$HOME/.bash_login.local"
